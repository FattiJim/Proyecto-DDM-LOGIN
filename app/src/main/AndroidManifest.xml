<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/buap_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/buap_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.Login"

        android:statusBarColor="@color/buap"


        tools:targetApi="31">
        <activity
            android:name=".MostrarActivity"
            android:exported="false" />
        <activity
            android:name=".EliminarActivity"
            android:exported="false" />
        <activity
            android:name=".ModificarActivity"
            android:exported="false" />
        <activity
            android:name=".AgregarActivity"
            android:exported="false" />
        <activity
            android:name=".MenuActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>